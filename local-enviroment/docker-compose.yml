name: crediya-auth-cs

services:
  postgres-db:
    image: postgres:16.8
    container_name: crediya-db-auth
    restart: always
    environment:
      POSTGRES_PASSWORD: pass123
      POSTGRES_DB: auth_db
    ports:
      - "5432:5432"
